{
  "name": "chalkstream",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "NODE_ENV=production parcel src/*.html",
    "build": "parcel build src/*.html && cp -r static/* dist",
    "generate-poster": "ffmpeg -loop 1 -i static/poster.png -c:v libx264 -t 6 -pix_fmt yuv420p -vf \"scale=640:360\" -f mpegts static/poster.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.9.3",
    "process": "^0.11.10",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.11.6",
    "ulid": "^2.3.0",
    "urlpattern-polyfill": "^9.0.0",
    "video.js": "^8.3.0"
  }
}